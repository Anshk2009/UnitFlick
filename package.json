{
  "name": "unitflick",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension that converts highlighted units and currencies.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "audit": "node tools/audit.js",
    "check": "npm test && npm run audit",
    "build": "python tools/build.py"
  }
}